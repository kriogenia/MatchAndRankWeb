import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles(() => ({
	link: {
		textDecoration: "none"
	},
	startButton: {
		width: "100%",
		margin: "10px 0"
	},
	subtitle: {
		textTransform: "initial"
	}
}));